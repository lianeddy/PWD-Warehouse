const apiUrl = "http://localhost:2000";
const apiUrl_user = `${apiUrl}/user`;
const apiUrl_product = `${apiUrl}/product`;
const apiUrl_cart = `${apiUrl}/cart`;
const apiUrl_admin = `${apiUrl}/admin`;
const primaryColor = "#f4f6ff";
const surfaceColor = "#61B15A";
const accentColor = "#F0A500";

export {
	apiUrl,
	apiUrl_user,
	apiUrl_product,
	surfaceColor,
	primaryColor,
	accentColor,
	apiUrl_admin,
	apiUrl_cart,
};
